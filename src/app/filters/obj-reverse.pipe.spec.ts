import { ObjReversePipe } from './obj-reverse.pipe';

describe('ObjReversePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjReversePipe();
    expect(pipe).toBeTruthy();
  });
});
